format = """$python$username$custom$hostname $directory $character
"""
right_format = "$git_branch $sudo"


[custom.at]
command = "echo '@'"
when = "true"
format = "[$output](bold red)"
unsafe_no_escape = true

# Icono del sistema operativo
[os]
disabled = false
format = '[$symbol](bold #442244) '
#format = '\[ [$symbol](bold #C266C3) \]'

[os.symbols]
Linux = ""
Gentoo = "󰣨"

# Usuario
[username]
show_always = true
format = '\[ [$user](bold #ffedcf)'

# Host / dominio
[hostname]
ssh_only = false
format = '[$hostname](bold #ffedcf) \]'

# Directorio actual
[directory]
format = '[  $path](bold yellow)'
#truncation_length = 3
#truncation_symbol = "…/"

# Rama git
[git_branch]
symbol = ""
format = '[ $symbol $branch](bold #766722)'
#format = '\[[ $symbol $branch](bold white) \]'

# Estado git
[git_status]
format = "[ (- $all_status$ahead_behind)](bold red)"

# Prompt
[character]
success_symbol = "[❯](bold green)"
error_symbol = "[❯](bold red)"


[python]
format = '[$virtualenv]($style) '
style = "#C266C3"

[sudo]
disabled = false
symbol = '󱚫 '
format = "[$symbol]($style)"
style = "bold #662656"
